schema: https://graphql.anilist.co
documents: "./src/**/*.graphql/anilist"
generates:
  ./src/generated/AnilistGraphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-query
    config:
      fetcher: graphql-request
